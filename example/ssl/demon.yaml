servers:
  $default:
    server:
      host: 127.0.0.1
      port: 443
      ssl:
        chain: chain.pem
        chain_password: password
        key: key.pem
        key_password: password

    routes:
      "/":
        text: "Hello, world!"
